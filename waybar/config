{
  "layer": "top",
  "position": "top",
  "height": 28,
  "margin-top": 6,
  "margin-left": 6,
  "margin-right": 6,

  "modules-left": ["hyprland/workspaces", "window"],
  "modules-center": ["clock"],
  "modules-right": ["tray", "cpu", "memory", "network", "pulseaudio", "battery"],

  "clock": {
    "interval": 1,
    "format": "{:%a %b %d  %H:%M:%S}",
    "tooltip-format": "{:%A, %B %d %Y  %H:%M:%S}"
  },

  "window": {
    "max-length": 60,
    "separate-outputs": true
  },

  "hyprland/workspaces": {
    "all-outputs": true,
    "format": "{name}",
    "on-click": "activate",
    "on-scroll-up": "hyprctl dispatch workspace +1",
    "on-scroll-down": "hyprctl dispatch workspace -1",
    "disable-scroll": false
  },

  
"cpu": {
  "interval": 5,
  "format": "{icon} {usage}%",
  "format-icons": [""]  // Nerd Font CPU
},

"memory": {
  "interval": 5,
    "format": "{icon} {used:0.1f} GiB / {total:0.0f} GiB",
  "format-icons": [""]  // FA microchip
},

  "pulseaudio": {
    "format": "{icon} {volume}%",
    "format-muted": "  muted",
    "format-icons": ["", "", ""],
    "on-click": "pavucontrol"
  },

  "network": {
    "format-wifi": "  {essid}",
    "format-ethernet": "  wired",
    "format-disconnected": "  offline",
    "tooltip": true
  },

  "battery": {
    "states": { "warning": 25, "critical": 10 },
    "format": "{icon} {capacity}%",
    "format-charging": " {capacity}%",
    "format-plugged": " {capacity}%",
    "format-alt": "{time}",
    "format-icons": ["", "", "", "", ""],
    "hide-if-unavailable": true
  },

  "tray": {
    "icon-size": 16,
    "spacing": 6
  }
}

