import QtQuick
import ".."

// Standard raised surface: panel background, hairline border, panel radius.
Rectangle {
    color: Theme.panelBg
    border.color: Theme.pillBorder
    border.width: 1
    radius: Theme.panelRadius
}
